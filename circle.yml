machine:
  timezone: UTC
  java:
    version: oraclejdk8
  services:
    - docker
  environment:
    PROJECT: "agora"
    ENV: "dev"
    REPO: "jmr18/testbuilds"

dependencies:
  cache_directories:
    - "~/.sbt"
    - "target/resolution-cache"
    - "target/streams"
    - "project/target/resolution-cache"
    - "project/target/streams"
  override:
    - docker info

test:
  override:
    - chmod +x docker/sbt-test.sh
    - docker run -v $(pwd):/${PROJECT} --name test broadinstitute/scala-baseimage /${PROJECT}/docker/sbt-test.sh
  post:
    - docker stop test
    - docker rm -v test

deployment:
  all:
    branch: .*
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASSWORD
      - chmod +x build/build.sh; build/build.sh
#  dev:
#    branch: develop
#    commands:
#      # todo: trigger other build
